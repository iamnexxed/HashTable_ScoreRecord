
public class Player
{
    // name
    public string name;

    // level
    public string level;

    // rank
    public string score;

    public Player(string getName, string getLevel, string getScore)
    {
        name = getName;
        level = getLevel;
        score = getScore;
    }
    
}
